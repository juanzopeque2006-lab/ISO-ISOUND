# Configuración de Lombok a nivel de proyecto
# Nota: Lombok no controla el formato de logs; esto lo gestiona SLF4J.
# Este archivo asegura consistencia del nombre del logger y evita herencia de configs externas.

config.stopBubbling = true
lombok.log.fieldName = log
# Puedes cambiar el logger por defecto si lo deseas (slf4j recomendado)
# lombok.log.config = slf4j
